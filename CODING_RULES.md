# コーディングルール

## ✅ チェックリスト

### 原則
- [ ] **DRY原則の徹底** - 重複コードを避け、再利用可能な関数/クラス/フックを作成する
- [ ] **マジックナンバーなし** - 数値は定数として定義し、意味のある名前を付ける
- [ ] **ネスト禁止、早期リターンの採用** - 深いネストを避け、ガード句で早期リターンする
- [ ] **if-elseの早期リターン検討** - if-else文を書く際は、早期リターンで解決できないか必ず検討する。早期リターンで解決できる場合は、else節を削除して早期リターンを使用する

### コメント
- [ ] **WHATではなくWHYを書く** - 何をしているかではなく、なぜそうしているかを説明する
- [ ] **ファイル冒頭に説明コメント** - 各ファイルの先頭に、そのファイルが何をしているかの説明を書く。そのファイルの全体にとってのどんな役割を担っているのか初見がわかること。

### 命名規則
- [ ] **変数名・関数名の省略禁止** - 慣習で明らかなもの（`i`, `j`, `x`, `y`など）以外は省略しない
- [ ] **引数名の省略禁止** - 関数・メソッドの引数名も省略せず、自己ドキュメント化された名前を使用する（例：`pManager` → `inputParamManager`, `DA` → `dataAccess`）
- [ ] **自己ドキュメント化** - 変数名・関数名を見ただけで意図が分かるようにする
- [ ] **Reactコンポーネントの命名** - コンポーネント名はPascalCase、フックは`use`で始める

### 変数の分類と使い分け
- [ ] **一切変更しないもの** - `const`で定義（定数、マジックナンバーの置き換え、設定値など）
- [ ] **変更しうるもの** - `let`で定義（計算途中の値、ループカウンタなど）。ただし、可能な限り`const`を使用する
- [ ] **ユーザーが変更できるもの** - 明確に命名し、設定値として扱う（パラメータ、設定オブジェクトなど）
- [ ] **固定値の意識** - 値の変更可能性を明確に区別し、適切な修飾子（`const`, `readonly`（クラスプロパティ）、`as const`（型推論））を使用する

### TypeScript特有のルール
- [ ] **型定義の明示** - 可能な限り型を明示し、型推論に頼りすぎない（特に関数の戻り値型、パブリックAPI）
- [ ] **`any`の使用禁止** - `any`は避け、`unknown`や適切な型定義を使用する。やむを得ない場合は`eslint-disable`と理由をコメントで説明
- [ ] **インターフェースの活用** - オブジェクトの形状を定義する際は`interface`または`type`を使用する
- [ ] **型アサーションの最小化** - 型アサーション（`as`）は必要な場合のみ使用し、理由をコメントで説明する

### エラー処理・デバッグ
- [ ] **デバッグしやすく** - ログ出力、エラーメッセージを適切に記述する
- [ ] **模範的なエラー処理** - 適切なtry-catch処理、バリデーション、エラーメッセージを実装する
- [ ] **エラーハンドリングの一貫性** - エラーオブジェクトの扱いを統一し、`Error`インスタンスを適切に処理する
